import 'package:flutter/material.dart';

class ProdectInfoPage extends StatelessWidget {
  const ProdectInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}